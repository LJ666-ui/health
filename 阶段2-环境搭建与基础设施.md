# 阶段2：环境搭建与基础设施（第3-4周）

## 阶段目标

- 本地开发环境全部就绪
- 双云服务器基础环境搭建完成
- 所有中间件本地+云端部署完成
- Nacos集群运行正常
- 数据库主从复制配置完成
- Ollama本地大模型可正常推理

## 阶段产出

1. 本地开发环境就绪（JDK/Maven/Node/Python/Docker）
2. 本地所有中间件容器运行（Redis/InfluxDB/MongoDB/MySQL/Nacos/Ollama）
3. 腾讯云服务器环境就绪（Docker/Nacos/MySQL主库/Redis主节点/Nginx）
4. 阿里云服务器环境就绪（Docker/Nacos/MySQL从库/InfluxDB/MongoDB）
5. 环境检测脚本（一键校验所有服务连通性）

---

## 第3周：本地开发环境与中间件搭建

### 本周总目标

本地Windows开发机所有环境搭建完成，所有中间件Docker容器正常运行

### 周一（Day 15）：本地基础开发环境配置

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-10:30 | JDK1.8安装 | 下载安装JDK1.8，配置JAVA_HOME、PATH环境变量，验证java -version | ❌必须自己下载安装和配置环境变量 |
| 10:30-12:00 | Maven3.6.3安装 | 下载解压Maven，配置MAVEN_HOME、settings.xml阿里云镜像、本地仓库路径 | ⚠️AI可给settings.xml配置内容，但下载解压和配置环境变量需自己操作 |
| 14:00-15:30 | Node.js16安装 | 安装Node.js16.20.2，配置npm淘宝镜像，验证node -v和npm -v | ❌必须自己下载安装 |
| 15:30-17:00 | Python3.12安装 | 安装Python3.12，配置pip阿里云镜像，创建虚拟环境 | ❌必须自己下载安装，AI可给pip镜像配置命令 |
| 19:30-21:30 | Git安装配置 | 安装Git，配置用户名邮箱，生成SSH Key，初始化项目仓库 | ⚠️AI可给Git配置命令，但安装和执行需自己操作 |

### 周二（Day 16）：Docker Desktop与本地MySQL

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | Docker Desktop安装 | 安装Docker Desktop for Windows，配置镜像加速器(阿里云/中科大)，验证docker version | ❌必须自己下载安装Docker Desktop |
| 14:00-17:30 | MySQL容器部署 | docker run部署MySQL8.0，配置端口3306映射、root密码、数据持久化卷、字符集utf8mb4，使用Navicat连接验证 | ⚠️AI可给docker run命令，提示词："请帮我生成docker run部署MySQL8.0的命令，端口3306，密码root123，数据持久化到/var/mysql/data，字符集utf8mb4"，但执行命令和Navicat连接需自己操作 |
| 19:30-21:30 | MySQL初始化 | 执行上周设计的建表SQL脚本，初始化用户表、角色表、权限表、设备表、日志表，插入基础数据 | ⚠️AI可帮写SQL脚本，但在Navicat中执行需自己操作 |

### 周三（Day 17）：本地Redis与InfluxDB

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | Redis容器部署 | docker run部署Redis7.0，配置端口6379映射、开启RDB持久化、设置密码，使用redis-cli连接验证 | ⚠️AI可给docker run命令，提示词："请帮我生成docker run部署Redis7.0的命令，端口6379，密码redis123，开启RDB持久化"，但执行和验证需自己操作 |
| 14:00-17:30 | InfluxDB容器部署 | docker run部署InfluxDB2.7，配置端口8086映射、管理员账号、初始组织与Bucket、数据保留策略，使用Web UI验证 | ⚠️AI可给docker run命令，提示词："请帮我生成docker run部署InfluxDB2.7的命令，端口8086，管理员admin/admin123，组织zhihealth，Bucket health_data"，但执行和Web UI验证需自己操作 |
| 19:30-21:30 | InfluxDB初始化 | 创建健康时序数据Bucket(health_data)，配置保留策略30天，创建API Token，验证数据写入与查询 | ❌必须自己在InfluxDB Web UI中操作 |

### 周四（Day 18）：本地MongoDB与Nacos

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | MongoDB容器部署 | docker run部署MongoDB6.0，配置端口27017映射、root账号、数据持久化，使用Navicat连接验证 | ⚠️AI可给docker run命令，提示词："请帮我生成docker run部署MongoDB6.0的命令，端口27017，root密码mongo123，数据持久化"，但执行和验证需自己操作 |
| 14:00-17:30 | Nacos容器部署 | docker run部署Nacos2.2.x单机模式，配置端口8848映射、MySQL作为持久化存储，访问Web UI验证 | ⚠️AI可给docker run命令，提示词："请帮我生成docker run部署Nacos2.2单机模式的命令，端口8848，MySQL持久化到本地MySQL8.0"，但执行和验证需自己操作 |
| 19:30-21:30 | MongoDB初始化 | 创建health数据库，初始化health_archive、ai_report、alert_log集合，插入测试文档验证 | ❌必须自己在Navicat或命令行中操作 |

### 周五（Day 19）：Ollama大模型与Python依赖

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | Ollama安装部署 | 安装Ollama for Windows，拉取通义千问7B模型(qwen2:7b)，验证ollama run qwen2:7b正常推理 | ❌必须自己下载安装Ollama，执行ollama pull qwen2:7b拉取模型 |
| 14:00-17:30 | Python依赖安装 | pip安装项目所需全部依赖：pandas、numpy、scikit-learn、torch、influxdb-client、pymongo、requests、jieba、matplotlib、flask | ❌必须自己执行pip install命令 |
| 19:30-21:30 | Ollama API测试 | 编写Python脚本调用Ollama REST API(http://localhost:11434/api/generate)，测试健康咨询推理，验证响应格式 | ✅AI可帮写测试脚本，提示词："请帮我编写Python脚本调用Ollama REST API，地址http://localhost:11434/api/generate，发送健康咨询问题并打印响应" |

### 周六（Day 20）：环境检测脚本与联调

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | 编写检测脚本 | 编写Python/Shell环境检测脚本，逐一校验MySQL/Redis/InfluxDB/MongoDB/Nacos/Ollama连通性 | ✅AI可帮写检测脚本，提示词："请帮我编写Python环境检测脚本，逐一校验MySQL(3306)/Redis(6379)/InfluxDB(8086)/MongoDB(27017)/Nacos(8848)/Ollama(11434)的连通性" |
| 14:00-17:00 | 全环境联调 | 运行检测脚本，修复所有连通性问题，确保本地环境100%可用 | ❌必须自己运行脚本和排查问题 |

### 周日（Day 21）：休息与学习

| 时间 | 任务 | 具体内容 |
|------|------|----------|
| 上午 | 休息 | 充分休息 |
| 下午 | 技术学习 | 阅读Spring Cloud Alibaba Nacos官方文档 |
| 晚上 | 预习 | 预习下周云服务器环境搭建 |

---

## 第4周：双云服务器环境搭建与集群配置

### 本周总目标

腾讯云+阿里云服务器环境搭建完成，Nacos集群、MySQL主从、Redis主从配置完成

### 周一（Day 22）：云服务器基础环境

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | 腾讯云初始化 | Xshell连接腾讯云2C4G，关闭防火墙、配置安全组(开放8080-8090/3306/6379/8848/27017/8086端口)、更新系统yum update | ⚠️AI可给Linux命令，提示词："请帮我列出CentOS7初始化需要的命令：关闭防火墙、开放端口8080-8090/3306/6379/8848/27017/8086、yum update"，但SSH登录执行需自己操作 |
| 14:00-17:30 | 阿里云初始化 | Xshell连接阿里云2C2G，关闭防火墙、配置安全组(开放对应端口)、更新系统 | ⚠️同上，AI给命令，你执行 |
| 19:30-21:30 | Docker安装 | 两台服务器分别安装Docker，配置镜像加速器、开机自启，验证docker version | ⚠️AI可给Docker安装命令，提示词："请帮我列出CentOS7安装Docker的完整命令，包括配置阿里云镜像加速器和开机自启"，但SSH执行需自己操作 |

### 周二（Day 23）：Nacos集群搭建

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | 腾讯云Nacos | Docker部署Nacos节点1(腾讯云)，配置MySQL持久化、集群参数cluster.conf | ⚠️AI可给docker run命令和Nacos配置，提示词："请帮我生成docker run部署Nacos2.2集群模式的命令，MySQL持久化，集群IP列表包含腾讯云和阿里云"，但执行需自己操作 |
| 14:00-17:30 | 阿里云Nacos | Docker部署Nacos节点2(阿里云)，配置连接同一MySQL、集群参数，验证双节点互相发现 | ⚠️同上 |
| 19:30-21:30 | 集群验证 | 访问Nacos Web UI验证集群节点列表，测试服务注册与配置下发 | ❌必须自己在浏览器访问Nacos Web UI验证 |

### 周三（Day 24）：MySQL主从集群

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | MySQL主库(腾讯云) | Docker部署MySQL8.0主库，配置server-id=1、开启binlog、创建同步账号repl | ⚠️AI可给完整的主库配置步骤和my.cnf，提示词："请帮我生成MySQL8.0主库的Docker部署命令和my.cnf配置，server-id=1，开启binlog，创建同步账号repl"，但执行需自己操作 |
| 14:00-17:30 | MySQL从库(阿里云) | Docker部署MySQL8.0从库，配置server-id=2、配置主库连接信息、启动同步，验证show slave status | ⚠️AI可给从库配置步骤，提示词："请帮我生成MySQL8.0从库配置，server-id=2，连接主库IP，启动同步的完整步骤"，但执行需自己操作 |
| 19:30-21:30 | 主从验证 | 主库创建数据库建表、插入数据，验证从库自动同步 | ❌必须自己在两台服务器上验证数据同步 |

### 周四（Day 25）：Redis主从与InfluxDB/MongoDB

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | Redis主从 | 腾讯云部署Redis主节点，阿里云部署Redis从节点，配置主从复制，验证数据同步 | ⚠️AI可给Redis主从配置步骤，提示词："请帮我生成Redis主从复制的配置步骤，腾讯云主节点+阿里云从节点"，但执行需自己操作 |
| 14:00-15:30 | InfluxDB(阿里云) | Docker部署InfluxDB2.7(阿里云)，配置端口8086、管理员账号、Bucket | ⚠️AI可给docker run命令，但执行需自己操作 |
| 15:30-17:00 | MongoDB(阿里云) | Docker部署MongoDB6.0(阿里云)，配置端口27017、root账号 | ⚠️AI可给docker run命令，但执行需自己操作 |
| 19:30-21:30 | 远程连接验证 | 本地Navicat/客户端远程连接云服务器数据库，验证读写正常 | ❌必须自己在Navicat中配置连接并验证 |

### 周五（Day 26）：Nginx与网络互通

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | Nginx部署(腾讯云) | Docker部署Nginx(腾讯云)，配置反向代理、负载均衡(upstream指向两台服务器的微服务) | ⚠️AI可给Nginx配置文件，提示词："请帮我编写Nginx反向代理配置，负载均衡到腾讯云和阿里云两台后端服务器"，但上传配置和重启Nginx需自己操作 |
| 14:00-17:30 | 网络互通测试 | 测试本地→腾讯云、本地→阿里云、腾讯云↔阿里云网络互通，确保所有端口可达 | ❌必须自己用ping/telnet/curl测试 |
| 19:30-21:30 | 安全加固 | 配置服务器SSH密钥登录、禁用root远程登录、安装fail2ban防暴力破解 | ⚠️AI可给安全配置命令，但执行需自己操作 |

### 周六（Day 27）：全环境联调检测

| 时间 | 任务 | 具体内容 | AI可用性 |
|------|------|----------|----------|
| 09:00-12:00 | 云环境检测 | 编写远程环境检测脚本，验证所有云服务可用性 | ✅AI可帮写检测脚本 |
| 14:00-17:00 | 联调测试 | 本地应用连接云服务器数据库/Nacos，验证跨网络访问正常 | ❌必须自己运行测试和排查问题 |

### 周日（Day 28）：休息与学习

| 时间 | 任务 | 具体内容 |
|------|------|----------|
| 上午 | 休息 | 充分休息 |
| 下午 | 技术学习 | 阅读Spring Boot+MyBatis-Plus开发文档 |
| 晚上 | 预习 | 预习下周微服务开发 |

---

## 阶段2验收标准

| 序号 | 验收项 | 标准 |
|------|--------|------|
| 1 | 本地JDK/Maven/Node/Python | 版本正确，命令行可正常执行 |
| 2 | 本地Docker中间件 | MySQL/Redis/InfluxDB/MongoDB/Nacos/Ollama全部容器运行正常 |
| 3 | 腾讯云服务器 | Docker/Nacos/MySQL主库/Redis主节点/Nginx运行正常 |
| 4 | 阿里云服务器 | Docker/Nacos/MySQL从库/InfluxDB/MongoDB运行正常 |
| 5 | Nacos集群 | 双节点互相发现，服务注册与配置下发正常 |
| 6 | MySQL主从 | 主库写入数据，从库自动同步 |
| 7 | Redis主从 | 主节点写入数据，从节点自动同步 |
| 8 | Ollama大模型 | 本地API调用正常，推理响应正常 |
| 9 | 网络互通 | 本地↔腾讯云↔阿里云三机网络互通 |

---

## 附录：实际ECS配置记录（2026-06-01至2026-06-02）

### A.1 阿里云ECS主节点配置（zhihealth-master）

**购买时间：** 2026-06-01 20:11

**购买配置：**
| 配置项 | 选择值 | 说明 |
|--------|--------|------|
| 付费类型 | 按量付费 | 按使用时长计费，可节省停机 |
| 地域 | 华北2（北京） | 与从节点同地域 |
| 实例规格 | ecs.e-c1m2.large (2C4G) | 经济型e，2核vCPU 4GB内存 |
| 镜像 | CentOS 7.9 64位 | 纯净版系统 |
| 系统盘 | ESSD Entry 40 GiB | 云盘类型 |
| 带宽峰值 | 5 Mbps (按使用流量) | 按实际流量计费 |
| VPC | zhihealth-vpc | 专有网络 |
| 交换机 | zhihealth-switch | 虚拟交换机 |
| 可用区 | 北京-可用区F | 与从节点同一可用区 |
| 安全组 | sg-2zeb06mtqv67qe0w2cm9 | 已有安全组 |
| 登录方式 | 自定义密码 | root / Lijan200527@ |

**实例信息：**
- 实例ID: i-2zeb06mtqv67qe0zovrk
- 公网IP: 182.92.1.136
- 私网IP: 172.16.0.67

**环境安装步骤：**

```bash
# 1. 安装Docker（使用阿里云镜像源）
yum install -y yum-utils && \
yum-config-manager --add-repo https://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo && \
yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 2. 配置镜像加速器
mkdir -p /etc/docker && tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com",
    "https://docker.m.daocloud.io",
    "https://dockerhub.timeweb.cloud",
    "https://docker.1panel.live"
  ]
}
EOF

# 3. 启动Docker
systemctl daemon-reload && systemctl enable docker && systemctl start docker

# 4. 部署MySQL 8.0
docker run -d \
  --name mysql \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=123456 \
  -v mysql_data:/var/lib/mysql \
  mysql:8.0 \
  --character-set-server=utf8mb4 \
  --collation-server=utf8mb4_unicode_ci

# 5. 部署Nacos 2.3.0（单机模式+MySQL持久化）
docker run -d \
  --name nacos \
  -p 8848:8848 \
  -p 9848:9848 \
  -e MODE=standalone \
  -e SPRING_DATASOURCE_PLATFORM=mysql \
  -e MYSQL_SERVICE_HOST=172.16.0.67 \
  -e MYSQL_SERVICE_PORT=3306 \
  -e MYSQL_SERVICE_DB_NAME=nacos_config \
  -e MYSQL_SERVICE_USER=root \
  -e MYSQL_SERVICE_PASSWORD=123456 \
  nacos/nacos-server:v2.3.0

# 6. 部署Redis 7-alpine（AOF持久化）
docker run -d \
  --name redis \
  -p 6379:6379 \
  -v redis_data:/data \
  redis:7-alpine redis-server --appendonly yes

# 7. 部署RabbitMQ 3-management-alpine
docker run -d \
  --name rabbitmq \
  -p 5672:5672 \
  -p 15672:15672 \
  rabbitmq:3-management-alpine
```

---

### A.2 阿里云ECS从节点配置（zhihealth-slave）

**购买时间：** 2026-06-02 10:05

**购买配置：**
| 配置项 | 选择值 | 说明 |
|--------|--------|------|
| 付费类型 | 按量付费 | 按使用时长计费 |
| 地域 | 华北2（北京） | 与主节点同地域 |
| 实例规格 | ecs.e-c1m2.large (2C4G) | 与主节点相同规格 |
| 镜像 | CentOS 7.9 64位 | 纯净版系统 |
| 系统盘 | ESSD Entry 40 GiB | 云盘类型 |
| 带宽峰值 | 5 Mbps (按使用流量) | 与主节点相同 |
| VPC | zhihealth-vpc | **与主节点同一VPC！** |
| 交换机 | zhihealth-switch | **与主节点同一交换机！** |
| 可用区 | 北京-可用区F | **与主节点同一可用区！** |
| 安全组 | sg-2zeb06mtqv67qe0w2cm9 | **与主节点同一安全组！** |
| 登录方式 | 自定义密码 | root / Lijan200527@ |

**实例信息：**
- 实例ID: i-2ze7jote79dwl74ykssz
- 公网IP: 39.105.129.207
- 私网IP: 172.16.0.68

**环境安装步骤：**

```bash
# 1. 安装Docker（与主节点完全相同的步骤）
yum install -y yum-utils && \
yum-config-manager --add-repo https://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo && \
yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 2. 配置镜像加速器（四个国内镜像源）
mkdir -p /etc/docker && tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": [
    "https://mirror.ccs.tencentyun.com",
    "https://docker.m.daocloud.io",
    "https://dockerhub.timeweb.cloud",
    "https://docker.1panel.live"
  ]
}
EOF

# 3. 启动Docker并验证
systemctl daemon-reload && systemctl enable docker && systemctl start docker

# 4. 验证安装结果
docker --version          # Docker version 26.1.4, build 5650f9b ✅
docker compose version     # Docker Compose version v2.27.1 ✅
```

---

### A.3 内网互通验证记录

**验证时间：** 2026-06-02

**测试环境：** 从节点(zhihealth-slave) VNC终端

**测试命令与结果：**

```bash
# 测试1：Ping主节点
[root@slave ~]# ping 172.16.0.67 -c 4
PING 172.16.0.67 (172.16.0.67) 56(84) bytes of data.
64 bytes from 172.16.0.67: icmp_seq=1 ttl=64 time=0.460 ms
64 bytes from 172.16.0.67: icmp_seq=2 ttl=64 time=0.251 ms
64 bytes from 172.16.0.67: icmp_seq=3 ttl=64 time=0.189 ms
64 bytes from 172.16.0.67: icmp_seq=4 ttl=64 time=0.194 ms

--- 172.16.0.67 ping statistics ---
4 packets transmitted, 4 received, 0% packet loss, time 3000ms   ✅ 通过！

# 测试2：连接主节点MySQL端口（先安装nc工具）
[root@slave ~]# yum install -y nc
[root@slave ~]# nc -zv 172.16.0.67 3306
Ncat: Version 7.50 ( https://nmap.org/ncat )
Ncat: Connected to 172.16.0.67:3306.                          ✅ 连接成功！
Ncat: 0 bytes sent, 0 bytes received in 0.01 seconds.
```

**验证结论：**
- ✅ 两台服务器内网ping通畅（0%丢包）
- ✅ MySQL端口3306可达
- ✅ VPC内网通信正常，可以开始部署微服务

---

### A.4 费用估算

| 项目 | 单价 | 月费用估算 |
|------|------|-----------|
| 主节点(2C4G) | ¥0.254160/时 | ≈¥180-183/月（按实际使用） |
| 从节点(2C4G) | ¥0.254160/时 | ≈¥180-183/月（按实际使用） |
| 带宽流量 | ¥0.08/GB | ≈¥1.2/月（预估10-15GB） |
| **合计** | - | **≈¥360-370/月** |

**节省建议：**
- 使用"节省停机模式"可降低约50%费用
- 停机后保留数据但停止计算资源收费
- 重启后继续使用，IP可能变化（建议转弹性公网IP）

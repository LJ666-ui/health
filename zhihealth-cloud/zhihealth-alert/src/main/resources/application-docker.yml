# Docker环境配置
server:
  port: 8086

spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
      import-check:
        enabled: false
  datasource:
    url: jdbc:mysql://zhihealth-mysql:3306/zhihealth?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root_2024_zhihealth
  redis:
    host: zhihealth-redis
    port: 6379

logging:
  level:
    com.zhihealth.alert: info

# Docker环境配置
server:
  port: 8082

spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
  datasource:
    url: jdbc:mysql://zhihealth-mysql:3306/zhihealth_device?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: root_2024_zhihealth
  redis:
    host: zhihealth-redis
    port: 6379
    database: 1

logging:
  level:
    com.zhihealth.device: info

# Docker环境配置 - 覆盖默认localhost为容器名
server:
  port: 8081

spring:
  cloud:
    nacos:
      discovery:
        enabled: false  # Docker网络gRPC问题，暂时禁用
      config:
        enabled: false  # Docker环境使用环境变量配置
  datasource:
    url: jdbc:mysql://zhihealth-mysql:3306/zhihealth_user?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: root_2024_zhihealth
  redis:
    host: zhihealth-redis
    port: 6379

logging:
  level:
    com.zhihealth.user: info
